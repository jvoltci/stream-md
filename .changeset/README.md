# Changesets

This directory contains changesets — small files that describe changes ready to be released.

When you make a user-facing change, run `npm run changeset` to create a new file here. The release workflow consumes these to bump versions and write the changelog.
